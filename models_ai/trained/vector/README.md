# Vector 모델 - 학습 파일 불필요

이 폴더에는 `.pt` 또는 `.pkl` 학습 파일이 없습니다.

## 이유

Vector 모델은 **실시간 학습** 방식을 사용합니다:

1. 과거 당첨 데이터를 로드할 때 자동으로 학습
2. K-Means 클러스터링과 KNN이 메모리에서 즉시 학습
3. 별도의 사전 학습 단계 불필요

## 소스 코드 위치

```
../src/vector/lotto_vector.py
```

## 다른 모델과의 차이

| 모델 | 학습 방식 | 파일 |
|------|----------|------|
| Transformer | 사전 학습 | `*.pt` |
| LSTM | 사전 학습 | `*.pt` |
| **Vector** | **실시간 학습** | **없음** |
